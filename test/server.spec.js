import expect from 'expect';
import { app } from '../server';

describe('server', () => {

  it('should have a server', () => {
    expect(app).toExist();
  });

  it('should save a todo', () => {
    
  });
  
});